name: Dependencies
run-name: ${{ github.actor }} is running Dependencies
on: [push]
jobs:
  Test-Dependencies:
    container:
      image: node:20-alpine3.19
    runs-on: ubuntu-latest
    steps:
      - run: npm install
